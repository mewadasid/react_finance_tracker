import React from 'react'

export default function tableCom({ tableData }) {
    return (
        <div>
            {tableData.map((item, index) => {
                return (
                    <table>
                        <thead>

                        </thead>
                        <tbody>
                            {prop.map}
                        </tbody>
                    </table>
                )
            }
    </div>
    )
}
